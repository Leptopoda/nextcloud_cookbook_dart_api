import 'package:test/test.dart';
import 'package:nc_cookbook_api/nc_cookbook_api.dart';

// tests for AppVersion
void main() {
  final instance = AppVersionBuilder();
  // TODO add properties to the builder and call build()

  group(AppVersion, () {});
}
